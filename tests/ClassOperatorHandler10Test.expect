#include <iostream>

enum class Color{
  red,
  blue,
  green
};
 
enum struct State: char {
  on,
  off,
};

int main()
{
  std::cout.operator<<(sizeof(Color)).operator<<(std::endl);
  std::cout.operator<<(sizeof(State)).operator<<(std::endl);
}


